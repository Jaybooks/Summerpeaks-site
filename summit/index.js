let articles=[],activeCategory='All',query='';const grid=document.getElementById('articleGrid'),count=document.getElementById('articleCount'),search=document.getElementById('articleSearch'),filters=[...document.querySelectorAll('.filter')];function render(){const q=query.trim().toLowerCase();const f=articles.filter(a=>(activeCategory==='All'||a.category===activeCategory)&&(!q||(`${a.title} ${a.description} ${a.category}`).toLowerCase().includes(q)));if(count)count.textContent=articles.length;grid.innerHTML=f.length?f.map((a,i)=>`<a class="article-card" href="/summit/articles/${a.slug}.html"><div class="card-meta"><span>${a.category}</span><span>${a.read}</span></div><h3>${a.title}</h3><p>${a.description}</p><div class="read-more">Read guide →</div></a>`).join(''):`<div class="no-results">No Summit guides match that search yet.</div>`}fetch('articles.json',{cache:'no-store'}).then(r=>r.json()).then(d=>{articles=d;render()});filters.forEach(b=>b.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));b.classList.add('active');activeCategory=b.dataset.category;render()}));search?.addEventListener('input',()=>{query=search.value;render()});